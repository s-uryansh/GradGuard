# Three data sources merged into one training set. 

- Tops up fingerprinting and exploit classes with synthetic samples since external datasets have none.
- Creates 200 samples of each class with realistic feature distributions.
- Reads live honeypot logs and extracts labeled samples from them.